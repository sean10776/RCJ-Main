#ifndef VALUE_H
#define VALUE_H
#include <Arduino.h>
extern uint16_t _Forward;
extern int maxspeed;
extern int Light_Per;
extern int Mid[10];

extern bool Motor_debug;//關閉Cpx的Error
extern bool debug;//Searchin 跟 Border的顯示
#endif